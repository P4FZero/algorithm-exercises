// Exercise001 - Counting with For Loop
// Write a program that displays numbers from 0 to 40,
// increasing by 5 each time, using a for loop,
// followed by the message "Finished!".

for (int count = 0; count <= 40; count += 5)
  Console.WriteLine(count);

Console.WriteLine("Finished!");
