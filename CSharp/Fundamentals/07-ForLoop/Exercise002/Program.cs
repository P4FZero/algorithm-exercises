// Exercise002 - Countdown with For Loop
// Write a program that displays numbers from 100 down to 0,
// decreasing by 10 each time, using a for loop,
// followed by the message "Finished!".

for(int count = 100; count >= 0; count -= 10)
  Console.WriteLine(count);

Console.WriteLine("Finished!");
