// Exercise001 - Initialize Array with Fixed Values
// Write a program that creates an array with 8 positions
// and fills all elements with the value 999.

int[] numbers = new int[8];

for (int index = 0; index < numbers.Length; index++)
  numbers[index] = 999;
